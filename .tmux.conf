unbind C-b
set-option -g prefix `
bind ` send-prefix

set -g default-terminal xterm-256color
set -s focus-events on
set -sg escape-time 10

if '[ -f ~/.tmux.conf.local ]' 'source ~/.tmux.conf.local'

set -g history-limit 30000
set -g base-index 1
setw -g pane-base-index 1
setw -g automatic-rename on
setw -g aggressive-resize on
set -g renumber-windows on
set -g set-titles on
set -g set-titles-string '❐ #h ▪ #S'

set -g status-interval 5

set -g monitor-activity on
set -g visual-activity off

setw -g xterm-keys on
setw -g mouse on
setw -g mode-keys vi
set -g status-keys vi

bind = select-layout even-horizontal

bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-h) || tmux select-pane -L"
bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-j) || tmux select-pane -D"
bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-k) || tmux select-pane -U"
bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-l) || tmux select-pane -R"

set -g status-justify centre
set -g status-style bg=colour12,fg=colour13
set -g status-left-style bg=colour13,fg=colour10
set -g status-left-length 35
set -g status-left '#[bg=green]#{?#{==:#{client_key_table},prefix}, PREFIX ,}#[default] ❐ #h ▪ #S #[bg=colour12,fg=colour13]'
set -g status-right ''
set -g automatic-rename-format '#I #{=-40:pane_title}'
set -g clock-mode-style 24
set -g window-status-current-style fg=red,bold
set -g window-status-activity-style fg=colour09
set -g window-status-separator '   '
set -g status-interval 60
