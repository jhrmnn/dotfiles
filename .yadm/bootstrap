#!/bin/bash

fish -c 'source ~/.config/fish/init.fish'
mkdir -p ~/.vim && ln -fns ../.local/share/nvim/site/autoload ~/.vim/autoload
mkdir -p ~/.local/share/bash
mkdir -p ~/.local/share/nvim/sessions
vim '+PlugUpdate' '+PlugClean!' '+PlugUpdate' '+qall'
